Remove this config in application.properties before deploy to production
```$xslt
management.endpoints.web.exposure.include=*
```
ใช้สำหรับเปิด monitoring application ทางที่ดีควรเลือกเฉพาะอย่างเท่าที่จะเปิดใช้